[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "exposuremap"
version = "0.1.0"
description = "Defensive exposure inventory helpers"
requires-python = ">=3.9"
